import { theme } from "antd";

export const themeConfig = {
  token: {
    bgSuccess: "#00B96B",
    headerBoxShadow: "rgba(40, 40, 40, 0.35) 2px 1px 5px 3px",
    headerBorderRadius: 7,
  },
  algorithm: theme.defaultAlgorithm,
};
